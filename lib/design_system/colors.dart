import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF3366FF);
  static const Color primaryDark = Color(0xFF1939B7);
  static const Color primaryLight = Color(0xFF598BFF);

  // Secondary colors
  static const Color secondary = Color(0xFF00C6B3);
  static const Color secondaryDark = Color(0xFF008F83);
  static const Color secondaryLight = Color(0xFF5CDFD3);

  // Neutral colors
  static const Color background = Colors.white;
  static const Color surface = Color(0xFFF5F5F5);
  static const Color divider = Color(0xFFE0E0E0);

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Feedback colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFE53935);
  static const Color info = Color(0xFF2196F3);
}
